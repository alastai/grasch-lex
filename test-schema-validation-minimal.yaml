# Minimal graphSchema document to test new schema structure
graphSchema:
  pathName: /test/minimal
  graphType:
    defaults:
      graphPreferredName: GRAPH
      nodePreferredName: NODE
      edgePreferredName: EDGE
