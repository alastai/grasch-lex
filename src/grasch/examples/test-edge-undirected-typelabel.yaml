# Test: Undirected edge using typeLabel: (generic synonym)
# Expected: PASS

graphSchema:
  pathName: /test/edge-undirected-typelabel
  graphType:
    propertyGraphDataModel:
      import: imports/lex-2026.0.3.2-property-graph-data-model-defaults.yaml
    
    nodeTypes:
      - nodeType:
          typeLabel: Person
    
    edgeTypes:
      - edgeType:
          undirected:
            typeLabel: FRIENDS_WITH
            between:
              nodeType:
                typeLabel: Person
            and:
              nodeType:
                typeLabel: Person
